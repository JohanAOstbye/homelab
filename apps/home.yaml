name: home
namespace: home
source:
  # Homepage dashboard defined in its own repo (config + manifests). The init
  # container in deploy/ syncs config/ into the homepage config dir. See its README.
  repository: https://git.ostbye.dev/johanaostbye/home.git
  revision: main
  path: deploy
  directory:
    recurse: true
