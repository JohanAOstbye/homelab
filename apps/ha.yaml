name: ha
namespace: ha
source:
  # Home Assistant is defined in its own repo (config + manifests). The init
  # container in deploy/ syncs config/ into /config on start. See that repo's README.
  repository: https://git.ostbye.dev/johanaostbye/ha.git
  revision: main
  path: deploy
  directory:
    recurse: true
