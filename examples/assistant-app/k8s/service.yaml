apiVersion: v1
kind: Service
metadata:
  name: assistant
  namespace: public
  labels:
    app: assistant
spec:
  selector:
    app: assistant
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP